# CI tests using GitHub Actions

name: CI

on:
  # trigger on push
  push:
    # to all branches
    branches:
      - '**'
    # ignore LICENSE, all .ignore and markdown files
    paths-ignore:
      - '*.md'
      - '*.ignore'
      - LICENSE
  pull_request:
    branches:
      - '**'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  
  #=======================================================================================================
  
  # Test via Docker and conda:
  SC_PREPROCESS_CI_TESTS:
    
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v2

      - name: INSTALL-NEXTFLOW
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/

      # full with docker
      - name: TEST-DOCKER_ALL
        run: |
          # building new index and run pipeline
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_new_idx,test_resources
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_new_idx,test_resources --only_fastqc
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_new_idx,test_resources --skip_fastqc

          # building new index and exit
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_new_idx,test_resources --only_idx

          # using existing index
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_existing_idx,test_resources
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_existing_idx,test_resources --only_fastqc
          NXF_VER=21.10.6 nextflow run main.nf -profile docker,test_with_existing_idx,test_resources --skip_fastqc
          

      # full with conda
      - name: TEST-CONDA
        run: |
          conda config --add channels r
          conda config --add channels conda-forge
          conda config --add channels bioconda
          NXF_VER=21.10.6 nextflow run main.nf -profile conda,test_with_new_idx,test_resources

      - uses: eWaterCycle/setup-singularity@v7
        with:
          singularity-version: 3.8.0
    
      # full with singularity
      - name: TEST-SINGULARITY
        run: NXF_VER=21.10.6 nextflow run main.nf -profile singularity,test_with_new_idx,test_resources